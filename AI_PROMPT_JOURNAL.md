# AI Prompt Journal

Record prompts used to learn and scaffold this project.

## Example prompt

- Prompt: "Create a minimal Rust Warp web server that serves an HTML page and a JSON endpoint."
- Notes: The model suggested using `warp`, `tokio`, and `serde_json` and provided a complete `main.rs` example which was adapted here.
